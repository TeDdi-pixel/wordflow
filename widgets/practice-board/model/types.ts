export type PracticeBoardProps = {
  unitSetId: string;
};

export type ControlsProps = {
  unitSetId: string;
};

export type StatisticsProps = {
  likesCount: number;
  savedUnitsCount: number;
  unitSetId: string;
};
