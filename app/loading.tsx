import Spinner from "@/shared/components/Spinner";

const Loading = () => {
  return (
    <main className="grow flex items-center justify-center mb-[100px]">
      <Spinner />
    </main>
  );
};

export default Loading;
