"use client";

import IconButton from "@/shared/ui/buttons/IconButton";
import BookmarkIcon from "@/shared/icons/unit/BookmarkIcon";

export const BookmarkButton = () => {
  return <IconButton icon={<BookmarkIcon />} />;
};
