"use client";

import IconButton from "@/shared/ui/buttons/IconButton";
import TipIcon from "@/shared/icons/unit/TipIcon";

export const HintButton = () => {
  return <IconButton icon={<TipIcon />} />;
};
